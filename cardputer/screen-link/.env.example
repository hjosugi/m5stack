# 実値を同じディレクトリの.envへ保存します。.envはGit管理されません。
WIFI_SSID=
WIFI_PASSWORD=
SCREEN_LINK_SERVER_HOST=192.0.2.1
SCREEN_LINK_SERVER_PORT=8765
SCREEN_LINK_TOKEN=

# 0: speakerを初期化しない（既定、完全mute）
# 1: speakerを初期化するが起動時mute。Mキーで切替可能
CARDPUTER_SPEAKER_ENABLED=0
